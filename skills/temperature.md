---
name: 即時溫度
description: 取得指定城市的即時溫度
---
只要瀏覽 `https://wttr.in/{encoded_city}?format=j1` 網頁就可以
取得指定程式的即時氣象資料：

- `{encoded_city}` 是城市的英文名稱經過 URL 編碼後的結果
- `format=j1` 會取得 JSON 格式的資料

你可以透過上述方法取得指定城市的即時溫度。